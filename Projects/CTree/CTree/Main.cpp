#include <stdio.h>
#include <stdlib.h>
#include "Linked_List.h"
#include "ClassTree_NodeTree.h"

int main()
{
	Tree<char> Arbolito;
	NodeTree<char>* puntero;
	NodeTree<char>* punteroBis;
	Arbolito.Add('F');
	/*punteroBis = Arbolito.Add('B', puntero);
	puntero = Arbolito.Add('G', puntero);
	puntero = Arbolito.Add('H', puntero);
	Arbolito.Add('I', puntero);
	Arbolito.Add('A', punteroBis);
	punteroBis = Arbolito.Add('D', punteroBis);
	Arbolito.Add('C', punteroBis);
	Arbolito.Add('E', punteroBis);
	List<char> lista;

	Arbolito.TreePreorderRecursive(&lista);

	bool resultado = (lista.index(0)->value == 'F');
	bool resultado1 = (lista.index(1)->value == 'B');
	bool resultado2 = (lista.index(2)->value == 'A');
	bool resultado3 = (lista.index(3)->value == 'D');
	bool resultado4 = (lista.index(4)->value == 'C');
	bool resultado5 = (lista.index(5)->value == 'E');
	bool resultado6 = (lista.index(6)->value == 'G');
	bool resultado7 = (lista.index(7)->value == 'H');
	bool resultado8 = (lista.index(8)->value == 'I');
	*/
	
	return 0;
}